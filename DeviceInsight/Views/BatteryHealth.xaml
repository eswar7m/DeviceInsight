<Page x:Class="DeviceInsight.Views.BatteryHealth"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="800"
      Title="Battery Health"
      Background="White">

    <Grid>
        <StackPanel Margin="20">
            <Button x:Name="BackButton" Style="{StaticResource NavigationButton}" Click="BackButton_Click" Content="← Back"/>
            <TextBlock Text="Battery Health Info" FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,20,0,20"/>
            <DataGrid ItemsSource="{Binding BatteryInfos}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserSortColumns="True"
                      BorderThickness="1" 
                      BorderBrush="#E0E0E0"
                      RowHeight="35">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Designed Capacity (mWh)" Binding="{Binding DesignedCapacity}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Full Charged Capacity (mWh)" Binding="{Binding FullChargedCapacity}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Battery Health (%)" Binding="{Binding BatteryHealth, StringFormat={}{0:F2}}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Estimated Time Left (mins)" Binding="{Binding EstimatedTimeLeft}" Width="180" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
